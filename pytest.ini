[pytest]
# Exclut les tests d'intégration par défaut (requièrent des credentials OAuth réels).
# Pour les lancer explicitement : pytest -m integration
addopts = -m "not integration"

markers =
    integration: tests nécessitant des ressources externes (Gmail OAuth, API tierces)
