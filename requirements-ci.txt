# Dépendances CI — Factur-X project
# ===================================
# Usage exclusif : pipeline GitHub Actions (validate-facturx.yml)
# Ne pas utiliser pour l'orchestrateur en production (voir orchestrator/requirements.txt)
#
# Pour régénérer avec des versions à jour :
#   pip install pip-tools
#   pip-compile requirements-ci.txt --upgrade

# Tests
pytest==8.3.2

# PDF — extraction texte natif + OCR Tesseract
PyMuPDF==1.24.14

# PDF — manipulation (injection OutputIntent + XMP pour PDF/A-3b)
pypdf==6.7.3

# Factur-X — embedding XML dans PDF, validation XSD (norme EN 16931)
factur-x==3.10.0

# XML — génération CII D16B/D22B avec namespaces
lxml==5.3.0

# PDF generation (utilisé par certains modules de test)
reportlab==4.2.2

# HTTP — appels API externes (non sollicités en tests unitaires, requis à l'import)
requests==2.32.3
