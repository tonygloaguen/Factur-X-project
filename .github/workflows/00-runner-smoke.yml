name: runner-smoke

on:
  workflow_dispatch:

jobs:
  smoke:
    permissions: {}
    runs-on: self-hosted
    steps:
      - name: Identity check
        run: |
          echo "Hostname:"
          hostname
          echo "User:"
          whoami
          echo "Kernel:"
          uname -a
          echo "Docker version:"
          docker --version
